# 05 — Content Pipeline

This document is the owner's operating manual for generating, reviewing, and publishing all card content in Chaos Creatures. Everything here is designed to run with a single command or a single click. No engineering judgment required.

**Depends on:** `00-game-design-master.md`, `01-battle-mechanics.md` (stat ranges, PP system, factions), `02-card-data-model.md` (CardTemplate schema), `03-prompt-templates.md` (FLUX and GPT-4o Mini prompt details)

**Infrastructure stack:** fal.ai (image generation), OpenAI API (text generation), Supabase (database), Cloudflare R2 (art storage + CDN), Railway (batch job runner — Node.js/TypeScript server side only)

**Client platform:** Native iOS only. Swift + SwiftUI + SpriteKit. App Store only. No Android, no React Native, no Expo.

**Admin dashboard:** Separate web application deployed on Railway. The owner accesses it in a browser. It is not part of the iOS app.

---

## 1. Launch Content Requirements

### 1a. Target Card Counts

| Type | Count Per Faction | Total (3 Factions) |
|---|---|---|
| Creatures | 100 | 300 |
| Spells | 17 | 51 |
| **Faction total** | **117** | **351** |
| Universal stabilizer cards | — | 7 (already designed in `01-battle-mechanics.md` Section 11; universal only — no faction-specific stabilizers at launch) |
| **Grand total** | | **358 card templates** |

**Practical launch target:** 351 faction cards + 7 universal = **358 cards.** Generation runs in batches of 50. The full launch run is 8 batches.

### 1b. Per-Card Requirements

Every card template needs all six of the following before it can be inserted into Supabase:

1. **Base art** — 768x1024 portrait PNG/WebP generated by fal.ai FLUX Dev (txt2img), stored in Cloudflare R2
2. **Card name** — 2-4 words, faction-appropriate, generated by GPT-4o Mini
3. **Flavor text** — 1-2 sentences under 120 characters, generated by GPT-4o Mini
4. **Stat assignment** — ATK, HP, CM cost (1-6), base instability (0-5), keywords (0-1 at Common), all within PP budget
5. **Card type and effect** — CREATURE, SPELL, or STABILIZER; spells and stabilizers have a structured effect definition
6. **Balance validation** — automated PP check, stat range check, instability coherence check all passing

### 1c. Content Distribution (Per Faction, 100 Creatures)

**Instability distribution:**

| Base Instability | Count | % | Role |
|---|---|---|---|
| 0 | 5 | 5% | Pure stability, zero board instability |
| 1 | 25 | 25% | Order-friendly, HP-heavy |
| 2 | 30 | 30% | Balanced, flexible |
| 3 | 25 | 25% | Chaos-leaning, ATK-heavy |
| 4 | 10 | 10% | Glass cannon |
| 5 | 5 | 5% | Extreme chaos, rare payoffs |

**CM cost distribution (creatures):**

| CM Cost | Count | PP Budget | ATK Range | HP Range |
|---|---|---|---|---|
| 1 | 15 | 3 | 1-2 | 1-2 |
| 2 | 20 | 5 | 1-3 | 2-4 |
| 3 | 25 | 7 | 2-4 | 2-5 |
| 4 | 20 | 9 | 2-5 | 3-6 |
| 5 | 15 | 11 | 3-6 | 3-7 |
| 6 | 5 | 13 | 4-7 | 4-8 |

**Keyword distribution (creatures):** approximately 40% of creatures have one keyword; 60% are vanilla. Keywords are distributed by instability: Shield/Taunt/Reach on instability 0-2, Flying/Piercing on instability 3-5, Deathtouch on any with ATK 1-2.

**Spells per faction (17 cards):**

| CM Cost | Count | Effect Power |
|---|---|---|
| 1 | 3 | Minor (deal 2 damage, +1/+1 this turn) |
| 2 | 4 | Moderate (deal 3 damage, draw 1, +2/+2 this turn) |
| 3 | 4 | Strong (deal 4 damage, destroy creature with 3 or less HP) |
| 4 | 3 | Premium (deal 5 damage, destroy any creature) |
| 5 | 2 | Powerful (deal 3 to all enemies, +2/+2 to all friendlies) |
| 6 | 1 | Game-swinging (deal 4 to all enemies, draw 3) |

**Stabilizers:** All 7 stabilizers are universal (shared across all factions), not faction-specific. They are fully designed in `01-battle-mechanics.md` Section 11 with effects, HP, and CM costs. Faction-specific stabilizers are reserved for future expansions.

---

## 2. The Generation Pipeline — End to End

### 2a. How It Works (One-Sentence Summary)

The owner runs one command in a terminal. The system generates all card art and text, validates everything automatically, and opens a review gallery in a browser. The owner clicks Approve or Reject on each card. Approved cards are inserted into Supabase. Rejected cards are regenerated with one click.

### 2b. Pipeline Architecture

```
[owner runs: npx ts-node scripts/generate-batch.ts --faction=ironwright --count=50]
         |
         v
[scripts/generate-batch.ts — runs on Railway or local Node.js]
  - reads card-specs/ironwright.csv
  - reads manifest: batch_results/run_TIMESTAMP/manifest.json
    (skips any card already marked complete in manifest)
  - for each pending card in batch:
      POST to fal.ai FLUX Dev --> get image URL
      (exponential backoff on fal.ai errors: 2s, 4s, 8s, 16s, max 4 retries)
      POST to OpenAI GPT-4o Mini --> get name + flavor_text
      run automated QA checks
      write result to manifest.json (marks card complete)
  - opens review gallery in browser at http://localhost:3001
         |
         v
[Review Gallery — http://localhost:3001]
  - Express server (Node.js, Railway-hosted or local)
  - displays all generated cards in a grid
  - owner clicks Approve / Reject (Art) / Reject (Name) / Edit
  - approved cards: uploaded to Cloudflare R2 --> inserted into Supabase
  - rejected cards: queued for regeneration (one click per card)
         |
         v
[Supabase card_templates table]
  - card is now live in the database
  - players can acquire this card from packs
```

### 2c. Resumable Pipeline — JSON Manifest

Every batch run writes its state to a JSON manifest at `batch_results/run_TIMESTAMP/manifest.json`. This file is the single source of truth for what has been generated, what has passed QA, and what is still pending.

**Manifest structure:**

```json
{
  "batch_id": "batch_2026_02_launch_ironwright_001",
  "faction": "ironwright",
  "started_at": "2026-02-16T10:00:00Z",
  "total_cards": 50,
  "cards": {
    "ironwright-001": {
      "status": "approved",
      "art_local_path": "batch_results/run_.../ironwright/card_001.webp",
      "art_r2_url": "https://pub-XXX.r2.dev/cards/ironwright/card_001.webp",
      "name": "Cogfang Stalker",
      "flavor_text": "Designed for pursuit, refined through conflict.",
      "fal_seed": 1234567890,
      "generated_at": "2026-02-16T10:05:00Z",
      "approved_at": "2026-02-16T11:00:00Z",
      "published_at": "2026-02-16T11:30:00Z"
    },
    "ironwright-002": {
      "status": "pending",
      "art_local_path": null,
      "art_r2_url": null,
      "name": null,
      "flavor_text": null,
      "fal_seed": null,
      "generated_at": null,
      "approved_at": null,
      "published_at": null
    }
  }
}
```

**Status values:**
- `pending` — not yet generated
- `generating` — API call in flight (set at start of call, cleared on completion or error)
- `qa_pass` — generated and passed automated checks, awaiting owner review
- `qa_fail` — generated but failed automated checks (shows in gallery with badge), auto-retry queued
- `approved` — owner approved, awaiting publish
- `rejected` — owner rejected, queued for regeneration
- `published` — uploaded to R2 and inserted into Supabase

**Resume behavior:** On startup, the pipeline reads the manifest and skips any card with status `approved`, `published`, or `qa_pass`. Cards with status `generating` are treated as `pending` (process may have been killed mid-call). This means killing the terminal and re-running the command resumes exactly where it left off. No cards are double-generated.

**fal.ai error handling with exponential backoff:**

```typescript
async function callFalWithRetry(
  endpoint: string,
  body: object,
  maxRetries = 4
): Promise<FalResponse> {
  let delay = 2000; // 2 seconds initial delay
  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      const response = await fetch(`https://fal.run/${endpoint}`, {
        method: 'POST',
        headers: {
          'Authorization': `Key ${process.env.FAL_KEY}`,
          'Content-Type': 'application/json'
        },
        body: JSON.stringify(body)
      });
      if (!response.ok) {
        const err = await response.text();
        throw new Error(`fal.ai HTTP ${response.status}: ${err}`);
      }
      return await response.json() as FalResponse;
    } catch (err) {
      if (attempt === maxRetries) throw err;
      console.warn(`fal.ai attempt ${attempt + 1} failed, retrying in ${delay}ms:`, err);
      await new Promise(resolve => setTimeout(resolve, delay));
      delay = Math.min(delay * 2, 32000); // cap at 32 seconds
    }
  }
  throw new Error('unreachable');
}
```

### 2d. Prerequisites — What the Owner Does Once

Before the first batch run, the owner must have these accounts and API keys in `/chaos-creatures/.env`:

```
FAL_KEY=...                    # from fal.ai dashboard
OPENAI_API_KEY=...            # from platform.openai.com
SUPABASE_URL=...              # from Supabase project settings
SUPABASE_SERVICE_ROLE_KEY=... # from Supabase project settings (not anon key)
CLOUDFLARE_ACCOUNT_ID=...     # from Cloudflare dashboard
CLOUDFLARE_R2_ACCESS_KEY=...  # from Cloudflare R2 API tokens
CLOUDFLARE_R2_SECRET_KEY=...  # from Cloudflare R2 API tokens
CLOUDFLARE_R2_BUCKET=chaos-creatures-card-art
CLOUDFLARE_R2_PUBLIC_URL=https://pub-XXXXXXXX.r2.dev
```

Claude Code builds the scripts. The owner only sets the `.env` values.

---

## 3. Exact API Calls

### 3a. fal.ai FLUX Dev — Base Card Art Generation

**Endpoint:** `POST https://fal.run/fal-ai/flux/dev`

**Authentication:** `Authorization: Key ${FAL_KEY}` header

**Request JSON (example: Ironwright 3-cost creature, instability 2, balanced):**

```json
{
  "prompt": "steampunk mechanical creature, brass and copper materials, exposed gears and clockwork mechanisms, riveted metal plating, steam vents, intricate precision engineering, industrial Victorian aesthetic, warm metallic tones with amber and rust highlights, glowing amber lenses, clockwork wolf, sleek predatory design, articulated brass leg joints with visible pistons, mechanical jaw with copper fangs, glowing amber optical sensors, mid-prowl stance, portrait orientation, centered creature filling 70 percent of frame, dramatic three-quarter view, industrial workshop background soft focus, clear distinct silhouette, card game art composition, eyes visible and facing viewer, dramatic directional lighting, fantasy card game art, high detail, professional digital illustration, sharp focus, vibrant warm metallic tones, Magic: The Gathering style composition, clean edges",
  "negative_prompt": "text, words, letters, watermarks, signatures, logos, borders, frames, NSFW, explicit content, gore, low quality, blurry, distorted anatomy, multiple heads, deformed limbs, floating objects, extra limbs, fused body parts, speech bubbles, comic panels, grid layout",
  "image_size": "portrait_4_3",
  "num_inference_steps": 35,
  "guidance_scale": 7.5,
  "num_images": 1,
  "enable_safety_checker": true,
  "output_format": "webp"
}
```

**Parameter reference:**

| Parameter | Value | Notes |
|---|---|---|
| `image_size` | `"portrait_4_3"` | Produces 768x1024 WebP — matches card aspect ratio used at all tiers |
| `num_inference_steps` | 35 | Quality vs. speed tradeoff. Use 30 for fast preview runs, 35 for production |
| `guidance_scale` | 7.5 | Higher = more prompt-adherent, less creative |
| `num_images` | 1 | One image per API call |
| `enable_safety_checker` | true | fal.ai built-in NSFW filter — automatically rejects unsafe outputs |
| `output_format` | `"webp"` | WebP for quality + smaller file size. Consistent with evolution art format in `03-prompt-templates.md` |

**Response JSON:**

```json
{
  "images": [
    {
      "url": "https://fal.media/files/rabbit/xxxx.webp",
      "width": 768,
      "height": 1024,
      "content_type": "image/webp"
    }
  ],
  "timings": {
    "inference": 4.2
  },
  "seed": 1234567890,
  "has_nsfw_concepts": [false],
  "prompt": "steampunk mechanical creature..."
}
```

**What the pipeline does with this response:**
1. Reads `images[0].url` — this is a temporary fal.ai CDN URL (expires in ~1 hour)
2. Downloads the image bytes immediately
3. Runs automated QA checks (Section 4)
4. If QA passes: stores in `batch_results/run_TIMESTAMP/ironwright/card_001.webp` locally and sets manifest status to `qa_pass`
5. After owner approval: uploads to Cloudflare R2 at `cards/ironwright/card_001.webp`
6. Records the R2 public URL as the permanent `art_url` in Supabase, sets manifest status to `published`

**Prompt construction rules (full algorithm):**

```
[FACTION_ART_PREFIX] + ", " + [CREATURE_DESCRIPTION] + ", " + [COMPOSITION_INSTRUCTION] + ", " + [QUALITY_TAGS]
```

These are the exact same constants defined in `03-prompt-templates.md` Sections 1.2. Use those definitions as the canonical source. Do not duplicate them here.

**Cost:** fal.ai FLUX Dev charges approximately $0.025 per image at 35 inference steps on the `portrait_4_3` size. Pricing is usage-based; check fal.ai dashboard for current rates as these can change.

**Rate limits:** fal.ai supports concurrent requests. The batch script sends up to 10 concurrent requests. For a 50-card batch at 10 concurrent: approximately 5-6 minutes total image generation time.

### 3b. OpenAI GPT-4o Mini — Card Name and Flavor Text

**Endpoint:** `POST https://api.openai.com/v1/chat/completions`

**Authentication:** `Authorization: Bearer ${OPENAI_API_KEY}` header

**Request JSON (example: Ironwright 3-cost, instability 2, ATK 3/HP 4, no keyword):**

```json
{
  "model": "gpt-4o-mini",
  "temperature": 0.8,
  "max_tokens": 150,
  "response_format": { "type": "json_object" },
  "messages": [
    {
      "role": "system",
      "content": "You are a card name and flavor text generator for Chaos Creatures, a fantasy card game. Always respond with valid JSON only."
    },
    {
      "role": "user",
      "content": "Generate a card name and flavor text for this card.\n\nFACTION: The Ironwright Collective\nFACTION VOICE: Industrial, pragmatic, precise. Names use mechanical compound words and references to metals, gears, and steam. Flavor text emphasizes function, craftsmanship, and the fusion of magic and machinery.\nCREATURE ARCHETYPE: clockwork wolf\nSTATS: 3 ATK / 4 HP, 3 chaos mote cost\nINSTABILITY: 2 (balanced)\nKEYWORDS: none\nVISUAL DESCRIPTION: balanced and composed, dynamic neutral stance, moderate steampunk detail\n\nGenerate:\n1. Card name: 2-4 words, faction-appropriate, memorable\n2. Flavor text: 1-2 sentences, under 120 characters, faction voice\n\nRespond ONLY with this JSON:\n{\"name\": \"...\", \"flavor_text\": \"...\"}"
    }
  ]
}
```

**Response JSON:**

```json
{
  "id": "chatcmpl-xxxx",
  "object": "chat.completion",
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "{\"name\": \"Cogfang Stalker\", \"flavor_text\": \"Designed for pursuit, refined through conflict. Its tolerances are tighter than its prey's nerves.\"}"
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 186,
    "completion_tokens": 38,
    "total_tokens": 224
  }
}
```

**What the pipeline does with this response:**
1. Parses `choices[0].message.content` as JSON
2. Extracts `name` and `flavor_text`
3. Validates: name is 3-30 characters, flavor text is under 120 characters
4. If validation fails: retries once with a stricter prompt ("The name must be 2-4 words and under 30 characters total. Do not explain, just output JSON.")
5. Stores alongside the card's art result in the batch manifest JSON

**System prompt additions by faction** (inserted into the system message alongside the base instruction):

```
Ironwright Collective:
"Industrial, pragmatic, precise. Names use mechanical compound words and references to metals, gears, and steam. Flavor text emphasizes function, craftsmanship, and the fusion of magic and machinery. Order tone = perfected engineering. Chaos tone = pushed beyond limits."

The Fey Courts:
"Ethereal, ancient, organic. Names use nature terms (Thorn, Root, Bloom, Grove) and fey titles (Warden, Huntress, Speaker). Flavor text is lyrical, timeless, referencing cycles and wildness. Order tone = harmony with nature, eternal patience. Chaos tone = wild hunt, primal fury."

The Demonic Kingdoms:
"Visceral, corrupted, direct. Names use dark materials (Ash, Bone, Blood, Shadow) and violent verbs (Reaver, Ripper, Render). Flavor text is ominous and foreboding. Order tone = controlled fury, dark pacts. Chaos tone = unbound carnage, apocalyptic."
```

**Cost:** GPT-4o Mini charges $0.15 per 1M input tokens and $0.60 per 1M output tokens. At roughly 200 input tokens and 40 output tokens per card: approximately $0.000054 per card, or $0.02 for all 358 cards. Text generation cost is effectively negligible.

**Using OpenAI Batch API for launch generation:** For the full 358-card launch run, use the OpenAI Batch API instead of real-time calls. This reduces cost by 50% (already negligible) and removes rate-limit concerns. The batch pipeline script automatically chooses Batch API for runs over 100 cards.

**Batch API request format:** Submit a `.jsonl` file where each line is one request:

```jsonl
{"custom_id": "ironwright-001", "method": "POST", "url": "/v1/chat/completions", "body": {"model": "gpt-4o-mini", "temperature": 0.8, "max_tokens": 150, "response_format": {"type": "json_object"}, "messages": [...]}}
{"custom_id": "ironwright-002", "method": "POST", "url": "/v1/chat/completions", "body": {"model": "gpt-4o-mini", "temperature": 0.8, "max_tokens": 150, "response_format": {"type": "json_object"}, "messages": [...]}}
```

Submit with: `POST https://api.openai.com/v1/batches`

```json
{
  "input_file_id": "file-xxxx",
  "endpoint": "/v1/chat/completions",
  "completion_window": "24h"
}
```

Batch API results arrive within 24 hours. For launch content, text generation can run the night before the art review session.

### 3c. Cloudflare R2 — Art Upload

After the owner approves a card in the review gallery, the pipeline uploads the local WebP to Cloudflare R2 using the AWS S3-compatible API (R2 is S3-compatible).

**Upload call (using AWS SDK v3 with R2 endpoint):**

```typescript
import { S3Client, PutObjectCommand } from "@aws-sdk/client-s3";

const r2 = new S3Client({
  region: "auto",
  endpoint: `https://${process.env.CLOUDFLARE_ACCOUNT_ID}.r2.cloudflarestorage.com`,
  credentials: {
    accessKeyId: process.env.CLOUDFLARE_R2_ACCESS_KEY,
    secretAccessKey: process.env.CLOUDFLARE_R2_SECRET_KEY,
  },
});

const key = `cards/${faction}/${cardId}.webp`;

await r2.send(new PutObjectCommand({
  Bucket: process.env.CLOUDFLARE_R2_BUCKET,
  Key: key,
  Body: imageBuffer,
  ContentType: "image/webp",
  CacheControl: "public, max-age=31536000, immutable",
}));

const publicUrl = `${process.env.CLOUDFLARE_R2_PUBLIC_URL}/${key}`;
// e.g. https://pub-XXXXXXXX.r2.dev/cards/ironwright/abc123.webp
```

The `publicUrl` is stored as `art_url` in the Supabase `card_templates` table. The `Cache-Control: immutable` header means Cloudflare CDN never re-fetches this image — it is cached globally forever, since base card art never changes.

### 3d. Supabase — Card Template Insert

After R2 upload, the pipeline inserts the approved card into Supabase using the service role key (bypasses Row Level Security for admin writes).

**Insert call:**

```typescript
import { createClient } from "@supabase/supabase-js";

const supabase = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY
);

const { data, error } = await supabase
  .from("card_templates")
  .insert({
    id: crypto.randomUUID(),
    name: "Cogfang Stalker",
    faction_id: "ironwright",
    card_type: "CREATURE",
    cm_cost: 3,
    base_instability: 2,
    base_attack: 3,
    base_health: 4,
    base_keywords: [],
    spell_effect: null,
    stabilizer_type: null,
    art_url: "https://pub-XXXXXXXX.r2.dev/cards/ironwright/abc123.webp",
    art_prompt: "steampunk mechanical creature...(full prompt stored here)",
    flavor_text: "Designed for pursuit, refined through conflict. Its tolerances are tighter than its prey's nerves.",
    batch_id: "batch_2026_02_launch_ironwright_001",
    approved_at: new Date().toISOString(),
    approved_by: "owner"
  });

if (error) {
  console.error("Insert failed:", error.message);
  // Pipeline logs this and continues with next card
  // Failed inserts are shown in a "retry" section of the review gallery
}
```

**The Supabase `card_templates` table schema** (Claude Code creates this migration):

```sql
create table card_templates (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  faction_id text not null references factions(id),
  card_type text not null check (card_type in ('CREATURE', 'SPELL', 'STABILIZER')),
  cm_cost integer not null check (cm_cost between 1 and 6),
  base_instability integer check (base_instability between 0 and 5),
  base_attack integer check (base_attack >= 0),
  base_health integer check (base_health >= 1),
  base_keywords text[] not null default '{}',
  spell_effect jsonb,
  stabilizer_type jsonb,
  art_url text not null,
  art_prompt text not null,
  flavor_text text not null,
  batch_id text,
  approved_at timestamptz not null default now(),
  approved_by text not null default 'owner',
  is_available boolean not null default true,
  created_at timestamptz not null default now()
);

create index idx_card_templates_faction on card_templates(faction_id);
create index idx_card_templates_available on card_templates(is_available);
```

---

## 4. Automated Quality Checks

These run automatically during batch generation, before any card reaches the review gallery. Checks that fail trigger automatic regeneration (up to 2 retries), then flag the card for the owner to manually reject or keep.

### 4a. Art Quality Checks

Run immediately after receiving the fal.ai response, before downloading the image:

| Check | Rule | On Failure |
|---|---|---|
| NSFW flag | `has_nsfw_concepts[0]` must be `false` in fal.ai response | Auto-regenerate with same prompt. After 2 failures: flag as NSFW_REJECTED in gallery |
| Image URL present | `images[0].url` must be a valid URL | Auto-retry API call with backoff |

Run after downloading the image:

| Check | Rule | On Failure |
|---|---|---|
| Resolution | Image must be 768x1024 pixels exactly | Auto-regenerate |
| File size | 50KB–3MB | Below 50KB = likely corrupted, regenerate. Above 3MB = unexpected, log and regenerate |
| Not blank | Less than 95% of pixels the same color | Auto-regenerate (blank = generation failure) |

The pipeline uses the `sharp` Node.js library for image checks. Sharp is installed as a project dependency by Claude Code.

### 4b. Text Quality Checks

Run immediately after receiving the OpenAI response:

| Check | Rule | On Failure |
|---|---|---|
| Valid JSON | Response parses as `{name, flavor_text}` | Retry once with explicit JSON instruction |
| Name length | 3-30 characters | Retry with "Name must be 2-4 words, under 30 characters total" added to prompt |
| Name not generic | Not "Creature", "Card", "Unit", "Monster" | Retry |
| Flavor text length | Under 120 characters | Retry with "Flavor text must be under 120 characters" added to prompt |

### 4c. Balance Checks

Run after stat assignment from the card spec CSV (before generation), and again after generation to confirm stats were recorded correctly:

```
PP budget check:
  required: (base_attack + base_health + keyword_pp_cost) == (cm_cost * 2 + 1)
  tolerance: ±1 PP allowed
  keyword PP costs: Shield=3, Lifesteal=2, Flying=2, Reach=1, Deathtouch=3, Taunt=1, Piercing=2

Stat range check (from 01-battle-mechanics.md Section 12):
  CM 1: ATK 1-2, HP 1-2
  CM 2: ATK 1-3, HP 2-4
  CM 3: ATK 2-4, HP 2-5
  CM 4: ATK 2-5, HP 3-6
  CM 5: ATK 3-6, HP 3-7
  CM 6: ATK 4-7, HP 4-8

Instability coherence check:
  instability 0-1: HP must be >= ATK
  instability 2: HP within 1 of ATK (either direction)
  instability 4-5: ATK must be >= HP

Deathtouch constraint:
  if keyword includes Deathtouch: ATK must be <= 2

Zero stat check:
  ATK must be >= 1
  HP must be >= 1

Common keyword limit:
  max 1 keyword at Common tier
```

Cards that fail balance checks are NOT auto-regenerated (art is fine, the spec has a bug). They are flagged in the review gallery with a red "BALANCE FAIL" badge showing the specific rule that failed. The owner can edit the stats directly in the gallery UI and revalidate.

### 4d. What the Owner Sees in the Gallery

Every card in the gallery has one of these status badges:

- **READY** (green) — all checks passed, click Approve to publish
- **BALANCE FAIL** (red) — stat math is wrong, shows which check failed, owner edits stats inline
- **ART REGEN** (yellow) — art failed checks and was auto-regenerated, shows attempt count
- **TEXT REGEN** (yellow) — name or flavor text was auto-regenerated
- **NSFW REJECTED** (red) — failed NSFW check 2 times, owner must manually reject and the pipeline skips it

---

## 5. Review Gallery UI

### 5a. Accessing the Gallery

The review gallery is a web application deployed on Railway (the admin dashboard). It is NOT part of the iOS app. After running the batch generation command, the gallery starts automatically at `http://localhost:3001` for local development. In production the admin dashboard is accessed at its Railway URL.

To view a previous batch: `npx ts-node scripts/review-gallery.ts --batch=batch_2026_02_launch_ironwright_001`

### 5b. Gallery Layout

The gallery is a Node.js Express server with server-rendered HTML. Claude Code builds this as part of the admin dashboard web application.

**Header bar:**
- Batch name and date
- Progress counters: `Approved: 23 / Pending: 19 / Rejected: 8 / Total: 50`
- "Publish All Approved" button (green) — inserts all approved cards into Supabase at once
- "Regenerate All Rejected" button (yellow) — queues all rejected cards for regeneration

**Card grid:**
- 4 columns on desktop, 2 on tablet
- Each card cell shows:
  - Art image (768x1024, displayed at proportional thumbnail)
  - Status badge (READY / BALANCE FAIL / etc.)
  - Card name (large, editable inline)
  - Faction + type + CM cost
  - Stats: `ATK X / HP Y / Instability Z`
  - Keywords (if any)
  - Flavor text (editable inline, small text below stats)
  - PP budget display: `PP used: X / PP budget: Y` (green if ±1, red if over)
  - Three action buttons:
    - **Approve** (green) — marks card ready to publish
    - **Reject Art** (red) — discards art, queues art regeneration
    - **Reject All** (red) — discards entire card, queues full regeneration

**Card detail modal (click any card art to expand):**
- Full-size art (768x1024)
- All card data
- Full edit form for stats, name, flavor text
- "Revalidate" button after editing
- Art prompt (collapsed by default, expandable for debugging)

**Bottom bar:**
- "Publish All Approved" button (duplicate of header)
- Cost summary: `Total API spend this batch: ~$X.XX`

### 5c. Actions and What They Do

**Approve:**
1. Marks card with `status: 'approved'` in the batch manifest JSON
2. Card turns green in the grid
3. Nothing is written to Supabase yet — "Publish All Approved" does that as a batch

**Reject Art:**
1. Marks art as rejected in manifest JSON
2. Queues a new fal.ai API call with the same prompt but a different seed
3. New art appears in the card cell within 30-60 seconds
4. Card returns to READY status if new art passes checks

**Reject All:**
1. Marks entire card as rejected in manifest JSON
2. Queues new fal.ai + OpenAI calls
3. New results replace the card cell

**Edit name or flavor text inline:**
1. Click on the text to make it editable
2. Type new text, press Enter or click away to save
3. Runs text validation checks automatically
4. Saves to manifest JSON (not Supabase until Publish)

**Edit stats inline:**
1. Click stat values to open a small edit popover
2. Adjust ATK, HP, instability, keywords via dropdowns and number inputs
3. PP budget display updates in real time
4. "Save stats" runs balance checks and updates the status badge

**Publish All Approved:**
1. For each approved card (in order):
   a. Uploads WebP from local storage to Cloudflare R2
   b. Inserts `card_templates` row into Supabase
   c. Updates manifest JSON status to `published`
   d. Updates card cell to show a "Published" checkmark
2. Shows a final summary: `Published 23 cards. 3 failed (see errors below).`
3. Failed inserts show a "Retry" button that re-attempts the R2 upload + Supabase insert

---

## 6. Batch Generation Commands

Claude Code creates these as `ts-node` scripts in the `scripts/` directory. There is no `package.json` npm-run shorthand on the client side; these run in the Railway/Node.js server environment or locally via `npx ts-node`.

### 6a. Generate a New Batch

```bash
npx ts-node scripts/generate-batch.ts --faction=ironwright --count=50
npx ts-node scripts/generate-batch.ts --faction=fey --count=50
npx ts-node scripts/generate-batch.ts --faction=demonic --count=50
```

Options:
- `--faction`: `ironwright`, `fey`, or `demonic`
- `--count`: number of cards to generate (default 50, max 100)
- `--type`: `creature`, `spell`, `stabilizer`, or `all` (default `all`)
- `--offset`: start from row N in the spec CSV (for generating a second batch from the same faction)
- `--dry-run`: validate all specs and build all prompts but make no API calls; outputs a cost estimate

### 6b. Resume an Interrupted Batch

If the terminal closes mid-run, resume exactly where it left off:

```bash
npx ts-node scripts/generate-batch.ts --resume --batch=batch_2026_02_launch_ironwright_001
```

The pipeline reads the manifest JSON, identifies all cards with status `pending` or `generating`, and runs only those. Cards already at `qa_pass`, `approved`, or `published` are skipped entirely. No API calls are wasted. No data is lost.

### 6c. Open the Review Gallery Without Generating

```bash
npx ts-node scripts/review-gallery.ts --batch=batch_2026_02_launch_ironwright_001
```

### 6d. Regenerate Specific Cards

```bash
npx ts-node scripts/regenerate-cards.ts --batch=batch_2026_02_launch_ironwright_001 --cards=ironwright-045,ironwright-038
```

Regenerates only the listed card IDs within a completed batch. Useful for bulk-rejecting art after review and regenerating all rejected cards at once.

### 6e. Publish an Approved Batch Without the Gallery

For automation (e.g., after reviewing in the gallery and closing it):

```bash
npx ts-node scripts/publish-batch.ts --batch=batch_2026_02_launch_ironwright_001
```

Uploads all approved cards to R2 and inserts into Supabase. Skips any cards not marked approved in the manifest.

---

## 7. Card Spec CSV Format

The batch generator reads card specs from `card-specs/{faction}.csv`. This is the source of truth for what gets generated. The owner fills this out (or Claude Code fills it based on the distribution tables in Section 1c).

### 7a. CSV Column Definitions

```csv
id,faction_id,card_type,cm_cost,base_instability,base_attack,base_health,keywords,creature_archetype,spell_effect_type,spell_effect_value,spell_effect_target,stabilizer_effect
```

| Column | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Unique ID for this card spec. e.g. `ironwright-001`. Used as filename prefix for art. |
| `faction_id` | string | yes | `ironwright`, `fey`, or `demonic` |
| `card_type` | string | yes | `CREATURE`, `SPELL`, or `STABILIZER` |
| `cm_cost` | integer 1-6 | yes | Chaos mote cost |
| `base_instability` | integer 0-5 | creatures only | Instability value. Leave blank for spells/stabilizers. |
| `base_attack` | integer | creatures only | ATK stat. Leave blank for spells/stabilizers. |
| `base_health` | integer | creatures only | HP stat |
| `keywords` | string | no | Pipe-separated keywords. e.g. `Shield` or `Flying|Reach` or empty |
| `creature_archetype` | string | creatures only | Short description for prompt. e.g. `clockwork wolf`, `mycelial guardian`, `infernal ravager` |
| `spell_effect_type` | string | spells only | `DEAL_DAMAGE`, `BUFF_ATK`, `BUFF_HP`, `HEAL`, `DRAW_CARD`, `DESTROY_CREATURE` |
| `spell_effect_value` | integer | spells only | Numeric value of effect (damage amount, buff amount, etc.) |
| `spell_effect_target` | string | spells only | `ANY_CREATURE`, `ENEMY_CREATURE`, `FRIENDLY_CREATURE`, `ENEMY_AVATAR`, `OWN_AVATAR` |
| `stabilizer_effect` | string | stabilizers only | Free-text description of aura effect |

### 7b. Example CSV Rows

```csv
ironwright-001,ironwright,CREATURE,3,2,3,4,,clockwork wolf,,,
ironwright-002,ironwright,CREATURE,1,3,2,1,Piercing,steam-driven dart,,,
ironwright-003,ironwright,CREATURE,4,1,2,6,Shield,armored war golem,,,
ironwright-004,ironwright,SPELL,2,,,,,DEAL_DAMAGE,3,ENEMY_CREATURE,
ironwright-005,ironwright,STABILIZER,3,,,,2 hp,,,,While on field: each of your creatures contributes -1 instability (minimum 0)
```

**PP budget check on ironwright-001:** ATK 3 + HP 4 + keywords 0 = 7 PP. CM cost 3 → budget = (3×2)+1 = 7. Passes.

**PP budget check on ironwright-002:** ATK 2 + HP 1 + Piercing (2 PP) = 5 PP. CM cost 1 → budget = (1×2)+1 = 3. FAILS. This spec is wrong — the pipeline catches this during the pre-generation validation pass and shows it in the dry-run report.

**PP budget check on ironwright-003:** ATK 2 + HP 6 + Shield (3 PP) = 11 PP. CM cost 4 → budget = (4×2)+1 = 9. FAILS by 2 PP. Needs adjustment to ATK 2 + HP 4 + Shield (3 PP) = 9 PP.

The pipeline validates all specs before making any API calls. It outputs a validation report listing all PP failures, stat-range failures, and instability-coherence failures. The owner fixes the CSV and re-runs.

### 7c. Pre-Built Spec Files

Claude Code generates the starter spec CSV files for all three factions based on the distribution tables in Section 1c. The owner reviews these for any obvious archetype gaps before running generation. The archetypes in these files are placeholders — the owner can rename them to anything that fits the faction's theme.

**Starter archetypes by faction:**

**Ironwright (creature archetypes):**
clockwork wolf, steam-powered golem, brass sentinel, cogwork spider, iron guardian, mechanical war hound, piston ram, gear wyrm, steam knight, copper automaton, clockwork raven, forge construct, valve titan, riveted warden, pressure golem, arc lightning drake, brass dragonling, hydraulic crusher, cogwork archer, industrial behemoth, steam juggernaut, tempered blade-arm, furnace elemental, clockwork assassin, wrought-iron colossus (plus 75 more following the same pattern)

**Fey Courts (creature archetypes):**
mycelial guardian, thornwood stalker, ancient treant, moonpetal sprite, wild hunt hound, fey lord, dryad warrior, living root serpent, glowing mushroom spirit, starborn protector, antlered fey knight, sporecloud elemental, bioluminescent jellyfish, vine construct, grove warden, autumn herald, moonlit huntress, wildbloom creature, rootcaller spirit, crystal moss golem (plus 80 more)

**Demonic Kingdoms (creature archetypes):**
corrupted demon, infernal warlord, hellfire elemental, bone construct, shadow fiend, ashclaw fiend, bonebreaker imp, emberhound, blood ritualist, infernal ravager, skeletal warlord, lava wyrm, shadow stalker, corrupted knight, flesh horror, charnel beast, abyssal servant, hellgate guardian, sin manifestation, doom herald (plus 80 more)

---

## 8. Full Launch Generation Plan

### 8a. Prerequisite Steps (Owner Does These Once)

1. Create accounts and set API keys in `.env` (see Section 2d)
2. Run `npm install` in the scripts project root to install Node.js dependencies (Claude Code does this, no action needed)
3. Run `supabase db push` to apply the `card_templates` table migration (one command)
4. Create the R2 bucket `chaos-creatures-card-art` in the Cloudflare dashboard (30 seconds)
5. Enable public access on the R2 bucket (Settings → Public Access → Allow)
6. Review the starter spec CSVs in `card-specs/` and make any archetype adjustments

### 8b. Step-by-Step Generation Sequence

The full launch run is 8 batches of 50 cards each. Run one batch at a time. Each batch takes about 30 minutes to generate and 30-60 minutes to review.

**Day 1 — Ironwright Collective:**

```bash
# Batch 1: Ironwright creatures, rows 1-50
npx ts-node scripts/generate-batch.ts --faction=ironwright --type=creature --count=50 --offset=0

# Review gallery opens automatically. Approve/reject cards.
# Click "Publish All Approved" when done.

# Batch 2: Ironwright creatures, rows 51-100
npx ts-node scripts/generate-batch.ts --faction=ironwright --type=creature --count=50 --offset=50

# Review and publish.

# Batch 3: Ironwright spells (17)
npx ts-node scripts/generate-batch.ts --faction=ironwright --type=spell --count=17

# Review and publish.
```

**Day 2 — The Fey Courts:** (same sequence as Day 1, faction=fey)

**Day 3 — The Demonic Kingdoms:** (same sequence as Day 1, faction=demonic)

**Day 4 — Universal Stabilizers (7 cards):**

The 7 universal stabilizer cards from `01-battle-mechanics.md` Section 11 are already fully designed with effects, HP, and CM costs. They need art and text generation only.

```bash
npx ts-node scripts/generate-batch.ts --faction=universal --type=stabilizer --count=7
```

A `card-specs/universal.csv` file pre-populated with these 7 cards' specs is created by Claude Code.

### 8c. What to Do When You Reject a Card

- **Reject Art** (art looks wrong): Click the button. New art regenerates automatically in ~30 seconds. If the second art is also bad, reject again. After 3 attempts you can force-approve by clicking "Keep Best Attempt" (shows the 3 side by side).
- **Reject Name** (name is generic or wrong): Click the name text, type a new name manually, press Enter. Or click "Regenerate Name" to get a new GPT-4o Mini suggestion.
- **Reject All** (both art and text are wrong): Click "Reject All". Queues full regeneration.
- **Edit Stats** (balance check failed): Click the stat values to edit them. The PP tracker shows in real time if you're within budget. Save when green. No regeneration needed — art and text stay.

### 8d. Expected Approval Rate and Regeneration Budget

Based on AI art generation norms:
- ~75-80% of cards will be approved on first generation
- ~15-20% will need art regeneration (wrong composition, wrong faction feel, artifacts)
- ~3-5% will need text regeneration (generic name, too-long flavor text)
- ~1-2% will be manually stat-edited (PP math error in the spec CSV)

For a 50-card batch: expect to approve ~38-40 immediately, regenerate ~8-10, and manually edit ~1-2. Review time per batch: 30-45 minutes.

---

## 9. App Store Asset Generation

All App Store submission assets are generated as part of the launch checklist. None of these require paid design tools.

### 9a. App Icon

**Required by Apple:** 1024×1024 PNG, no alpha channel, no rounded corners (Apple applies rounding).

**Generation process:** Use fal.ai FLUX Dev with a dedicated app icon prompt. Generate 5 candidates and pick the best.

**App icon prompt:**

```
Chaos Creatures game logo, fantasy collectible card game icon, dramatic D20 die at center radiating Order and Chaos energy simultaneously, blue-white crystalline Order energy on one side, red-purple crackling Chaos energy on the other side, three small creature silhouettes — steampunk mechanical, ethereal fey, dark demonic — arranged around the die, dark atmospheric background, vibrant contrast, centered composition, professional game icon design, no text, bold and striking, square format, high detail, digital illustration
```

**fal.ai request for app icon:**

```json
{
  "prompt": "(prompt above)",
  "negative_prompt": "text, words, letters, watermarks, borders, frames, NSFW, low quality, blurry",
  "image_size": "square_hd",
  "num_inference_steps": 40,
  "guidance_scale": 8.0,
  "num_images": 1,
  "enable_safety_checker": true,
  "output_format": "png"
}
```

Note: `square_hd` produces 1024×1024. Use `output_format: "png"` here (not WebP) as Apple requires PNG for the app icon.

**Script:**

```bash
npx ts-node scripts/generate-app-icon.ts
# Generates 5 candidates in app-store-assets/icons/
# Owner opens folder in Finder and picks the best one
# Rename chosen file to AppIcon-1024.png
```

**After choosing:** Place `AppIcon-1024.png` in `ChaosCreatures/Assets.xcassets/AppIcon.appiconset/` and update `Contents.json` to reference it. Xcode generates all required icon sizes from this single source file using the "Single Size" option in the asset catalog.

**Cost:** 5 icon candidates × $0.025 = $0.13

### 9b. App Store Screenshots

**Required by Apple:** At a minimum, screenshots for iPhone 6.7-inch (iPhone 15 Pro Max) and iPhone 6.5-inch (iPhone 14 Plus) display sizes. Screenshots are taken of the actual running app.

**Method: Fastlane snapshot + Xcode UI Tests**

Fastlane snapshot automates screenshot capture by driving Xcode UI Tests. Claude Code sets this up as follows:

**1. Install Fastlane (owner runs once):**

```bash
brew install fastlane
```

**2. Initialize in the iOS project root:**

```bash
cd /Users/alexali/Projects/chaos-creatures/ChaosCreatures
fastlane snapshot init
```

This creates `fastlane/Snapfile` and `fastlane/SnapshotHelper.swift`.

**3. Snapfile configuration:**

```ruby
# fastlane/Snapfile
devices([
  "iPhone 15 Pro Max",
  "iPhone 14 Plus"
])

languages([
  "en-US"
])

scheme("ChaosCreatures")
output_directory("./fastlane/screenshots")
clear_previous_screenshots(true)
```

**4. UI Test target for screenshots (`ChaosCreaturesUITestsScreenshots`):**

Claude Code writes the UI test class that navigates to each required screen and calls `snapshot("screen_name")`:

```swift
// ChaosCreaturesUITests/ScreenshotTests.swift
import XCTest

final class ScreenshotTests: XCTestCase {

    override func setUp() {
        super.setUp()
        let app = XCUIApplication()
        setupSnapshot(app)
        app.launch()
    }

    func testScreenshot01_MainMenu() {
        let app = XCUIApplication()
        snapshot("01_MainMenu")
    }

    func testScreenshot02_BattlefieldInGame() {
        let app = XCUIApplication()
        // Navigate to a pre-seeded game state
        app.buttons["Debug_LoadSampleMatch"].tap()
        snapshot("02_BattlefieldInGame")
    }

    func testScreenshot03_CardCollection() {
        let app = XCUIApplication()
        app.buttons["Collection"].tap()
        snapshot("03_CardCollection")
    }

    func testScreenshot04_CardEvolution() {
        let app = XCUIApplication()
        app.buttons["Debug_LoadEvolutionScreen"].tap()
        snapshot("04_CardEvolution")
    }

    func testScreenshot05_DeckBuilder() {
        let app = XCUIApplication()
        app.buttons["DeckBuilder"].tap()
        snapshot("05_DeckBuilder")
    }
}
```

**5. Run screenshots:**

```bash
fastlane snapshot
```

Fastlane launches the Simulator for each device size, runs the UI tests, and saves screenshots to `fastlane/screenshots/en-US/`. The owner uploads these directly in App Store Connect.

**Required screenshot count:** A minimum of 3 screenshots per device size. 5 are recommended. Aim for:
1. Main menu / title screen
2. Active battlefield mid-game
3. Card collection grid
4. Card evolution reveal animation frame
5. Deck builder

**Cost:** $0 (Simulator time only)

### 9c. Privacy Policy and Terms of Service

Apple requires a publicly accessible privacy policy URL for any app submitted to the App Store.

**Hosting:** Cloudflare Pages (free tier). Two static HTML files.

**Files to create:**

- `static-pages/privacy-policy.html` — describes what data is collected (account email, match data, purchase history via StoreKit 2)
- `static-pages/terms-of-service.html` — standard ToS for a card game

Claude Code writes both files with accurate content based on the game's actual data practices. The owner deploys them to Cloudflare Pages by connecting the GitHub repo to Cloudflare Pages and setting the build output to `static-pages/`.

**Published URLs:**
- `https://chaos-creatures.pages.dev/privacy-policy`
- `https://chaos-creatures.pages.dev/terms-of-service`

These URLs go into App Store Connect under "App Information → Privacy Policy URL."

**Cost:** $0 (Cloudflare Pages free tier)

### 9d. App Store Description Copy

Claude Code generates the App Store description text. It is stored in `app-store-assets/description.md` and copied into App Store Connect by the owner.

**Required fields in App Store Connect:**

| Field | Content | Length Limit |
|---|---|---|
| App Name | Chaos Creatures | 30 chars |
| Subtitle | Evolving Card Battle Game | 30 chars |
| Promotional Text | Build your deck, roll the die, and watch your cards evolve through Order and Chaos. | 170 chars |
| Description | Full description (see below) | 4000 chars |
| Keywords | card game, card battle, deck builder, collectible, strategy, RPG, chaos, evolution, fantasy | 100 chars |
| Support URL | https://chaos-creatures.pages.dev/support | — |
| Marketing URL | https://chaos-creatures.pages.dev | — |

**Description template** (fill in with actual details, then copy to App Store Connect):

```
Chaos Creatures is a collectible card game where every card's art evolves through play.

CORE GAMEPLAY
Build a 20-card deck from three factions: the industrial Ironwright Collective, the ancient Fey Courts, or the infernal Demonic Kingdoms. Each turn opens with a D20 Chaos Roll — your instability rating determines whether you trigger Order events (healing, shielding, buffing) or Chaos events (damage bursts, random effects). Master the tension between stability and power.

LIVING CARD ART
Every card you play earns evolution energy. When a card evolves, its art is redrawn by AI — changing with the energy you've channeled into it. Order evolutions refine and crystallize. Chaos evolutions fracture and empower. No two cards evolve the same way.

THREE FACTIONS
- Ironwright Collective: Precision and industry. Augment mechanics boost your own creatures.
- Fey Courts: Ancient and wild. Bond mechanics link creatures for shared benefits.
- Demonic Kingdoms: Power at any cost. Corruption mechanics trade HP for overwhelming strength.

COLLECT AND EVOLVE
Cards evolve through five tiers: Common, Uncommon, Rare, Epic, Legendary. Each evolution changes the art and upgrades the stats. Your Legendary card is unlike anyone else's in the world.

NO PAY-TO-WIN
Cards are earned through play. No randomized card packs. Spend Chaos Dust (earned in-game) to acquire specific cards. Subscriptions unlock higher-quality evolution art — not stronger cards.
```

### 9e. Age Rating and Privacy Nutrition Labels

**Age Rating questionnaire answers (fill in App Store Connect):**

| Question | Answer |
|---|---|
| Cartoon or Fantasy Violence | Mild |
| Realistic Violence | None |
| Profanity or Crude Humor | None |
| Mature or Suggestive Themes | None |
| Horror or Fear Themes | Infrequent or Mild |
| Gambling | None (no randomized card packs) |
| Alcohol, Tobacco, or Drug Use | None |
| Medical or Treatment Information | None |
| Sexual Content | None |

**Expected age rating result:** 9+

**Privacy Nutrition Labels (App Store Connect → App Privacy):**

| Data Type | Collected? | Purpose | Linked to Identity? | Used for Tracking? |
|---|---|---|---|---|
| Email Address | Yes | Account creation | Yes | No |
| User ID | Yes | Match history, progress sync | Yes | No |
| Purchase History | Yes | Subscription management (StoreKit 2) | Yes | No |
| Gameplay Data | Yes | Balance analytics (PostHog), match history | No | No |
| Crash Data | Yes | Xcode Organizer / PostHog error tracking | No | No |

### 9f. App Store Launch Checklist

Run this checklist before submitting to App Store Review:

```
App Icon
[ ] AppIcon-1024.png placed in Assets.xcassets (1024x1024 PNG, no alpha)
[ ] All Xcode-generated icon sizes build without warnings

Screenshots
[ ] 5 screenshots for iPhone 15 Pro Max (6.7-inch)
[ ] 5 screenshots for iPhone 14 Plus (6.5-inch)
[ ] Screenshots show actual gameplay, not placeholder screens
[ ] No status bar showing battery or time (use simulator with clean status bar)

Static Pages
[ ] privacy-policy.html deployed to Cloudflare Pages and publicly accessible
[ ] terms-of-service.html deployed to Cloudflare Pages and publicly accessible
[ ] Both URLs load correctly at chaos-creatures.pages.dev/...

App Store Connect Metadata
[ ] App Name, Subtitle, Promotional Text, Description filled in
[ ] Keywords filled in (under 100 characters)
[ ] Support URL and Marketing URL set
[ ] Age rating questionnaire submitted (expected: 9+)
[ ] Privacy Nutrition Labels completed
[ ] Version number matches Xcode build (e.g., 1.0.0, build 1)

Content
[ ] All 358 card templates published to Supabase
[ ] All card art URLs reachable on Cloudflare R2 (run: npx ts-node scripts/pre-release-check.ts)
[ ] Regression tests passing (see Section 11c)
[ ] Balance simulation run on full card pool (Section 11a)

Technical
[ ] App builds for distribution target (Xcode → Product → Archive)
[ ] No SwiftUI previews or debug UI visible in production build
[ ] StoreKit 2 subscription products created in App Store Connect
[ ] Subscription product IDs match constants in Swift code
[ ] Xcode Cloud build passing (or manual archive and upload via Xcode Organizer)

Legal
[ ] Apple Developer Program membership active ($99/year)
[ ] App Store Review Guidelines checklist reviewed
[ ] EULA: use Apple's standard EULA (default) unless custom EULA is needed
```

---

## 10. Estimated Time and Cost for Full Launch

### 10a. API Cost Breakdown (All Launch Content + Store Assets)

**Art generation (fal.ai FLUX Dev at ~$0.025/image):**

| Item | Count | Cost per Image | Total |
|---|---|---|---|
| Faction creatures (3 × 100) | 300 | $0.025 | $7.50 |
| Faction spells (3 × 17) | 51 | $0.025 | $1.28 |
| Universal stabilizers | 7 | $0.025 | $0.18 |
| Regenerations (~20% of 358) | ~72 | $0.025 | $1.80 |
| App icon candidates | 5 | $0.025 | $0.13 |
| **Art total** | **435** | | **$10.89** |

**Text generation (OpenAI GPT-4o Mini Batch API):**

| Item | Count | Cost per Card | Total |
|---|---|---|---|
| All 358 cards (name + flavor text) | 358 | ~$0.000054 | $0.02 |
| Regenerations | ~30 | ~$0.000054 | <$0.01 |
| **Text total** | | | **~$0.02** |

**Storage (Cloudflare R2):**

| Item | Size | Cost |
|---|---|---|
| 358 WebPs × ~300KB avg | ~107MB | $0.00/month (R2 free: 10GB storage) |
| CDN egress (R2 is free) | — | $0.00 |

**App Store assets:**

| Item | Cost |
|---|---|
| Screenshots via Fastlane/Xcode Simulator | $0.00 |
| Privacy Policy + ToS on Cloudflare Pages | $0.00 |
| App Store description copy | $0.00 |

**Infrastructure first month:**

| Service | Tier | Monthly Cost |
|---|---|---|
| Supabase | Free tier (500MB DB, 2GB bandwidth) | $0.00 |
| Railway (batch pipeline server) | Hobby plan | $5.00 |
| Cloudflare R2 | Free tier (10GB storage, 1M reads) | $0.00 |
| fal.ai | Usage-based | (included in art total above) |
| OpenAI | Usage-based | (included in text total above) |
| Apple Developer Program | Annual | $99.00 |
| PostHog | Free tier (1M events/month) | $0.00 |

**Total one-time launch content cost: approximately $10.91**

**Total first-month infrastructure: approximately $5.00 (Railway) + $99.00 (Apple) = $104.00**

**Total spend to launch: approximately $114.91**

This fits comfortably within the $300 budget. Remaining headroom (~$185) covers:
- Supabase Pro upgrade if free tier is exceeded during development (~$25/month)
- Additional fal.ai generation if rejection rates are higher than 20%
- Domain name for custom URL (optional, ~$10/year)
- Any unexpected service fees

### 10b. Time Breakdown

| Phase | Duration | Notes |
|---|---|---|
| Review and adjust spec CSVs | 1-2 hours | One-time setup. Owner reviews archetypes, makes any changes. |
| Text generation (Batch API for all 358 cards) | Submitted overnight, results in ~12 hours | Submit before bed, results ready in the morning |
| Art generation per batch of 50 (10 concurrent) | 5-7 minutes | fal.ai is fast |
| Gallery review per batch of 50 | 30-45 minutes | Owner reviews and approves/rejects |
| Regeneration wait per batch | 2-5 minutes | Auto-regenerates rejected cards |
| Publish per batch | 2-3 minutes | Upload to R2 + Supabase insert |
| **Per-batch total (generation + review + publish)** | **~45-60 minutes** | |
| **Full launch (8 batches × 60 minutes)** | **~8 hours** | Spread over 3-4 days |
| App icon generation and selection | 15 minutes | Generate 5, pick best |
| Fastlane screenshot setup | 1-2 hours | One-time Claude Code task |
| Fastlane snapshot run | 15 minutes | Automated |
| App Store Connect metadata entry | 1-2 hours | Manual form filling |

**Practical schedule:** Run one or two batches per day over 4 days. Each session is about 1-2 hours. Total calendar time: 4-5 days from first batch to all cards published.

---

## 11. Seasonal Content Releases

### 11a. Post-Launch Content Cadence

| Release Type | Frequency | New Cards | Pipeline Run |
|---|---|---|---|
| Quarterly card packs | Every 3 months | 20-30 creatures per faction (60-90 total) | Same batch pipeline, smaller count |
| Balance patches | Monthly | 0 new cards (stat edits only) | Direct Supabase edit via admin UI |
| Seasonal events | Every 6 weeks | 1-3 avatar portraits | Single batch of 3 |
| New faction | Once per year | ~120 cards + 5 avatars | Full launch pipeline run |

### 11b. Quarterly Card Pack Pipeline

Exactly the same as launch content generation, just smaller:

```bash
# Q1 expansion, Ironwright, 25 new creatures
npx ts-node scripts/generate-batch.ts --faction=ironwright --type=creature --count=25
# Review and publish. ~45 minutes total.
```

New cards in the CSV get new IDs that don't conflict with launch cards (`ironwright-q1-001`, etc.). They are inserted into the same `card_templates` table and immediately become available in packs.

**Cost per quarterly release:** 60-90 cards × $0.025 art + negligible text + 20% regen buffer = approximately $2-3 per quarterly release.

### 11c. Balance Patches — Admin UI

Balance patches modify existing `card_templates` records. The owner never edits Supabase directly. Instead, Claude Code builds a balance admin screen in the admin dashboard web application at `/admin/balance`:

1. Shows all published cards in a searchable table
2. Owner searches for a card by name
3. Owner edits ATK, HP, instability, or CM cost inline
4. "Validate Change" button runs PP check and stat range check before saving
5. "Save Change" updates the Supabase row
6. A migration script runs automatically after save: updates all `card_instances` that reference this template, recalculating `current_attack`, `current_health`, and `instability_value` based on the new base stats plus each card's existing evolution history

**The migration script (critical — run automatically on every balance patch save):**

```sql
-- After updating card_templates set base_attack = 4 where id = 'ironwright-001'
-- This recalculates all card_instances that reference this template

UPDATE card_instances
SET current_attack = (
  SELECT base_attack FROM card_templates WHERE id = card_instances.template_id
) + COALESCE(evolution_attack_gain, 0) + COALESCE(modifier_attack_gain, 0)
WHERE template_id = 'ironwright-001';
```

The admin UI shows a preview of how many card instances will be affected before the owner confirms the change.

**How balance patches interact with evolved cards:** Template base stat changes propagate. Evolution gains stack on top of the new base. A card that was 3 ATK → evolved to 5 ATK (gained +2) becomes 6 ATK if the template is buffed to 4 ATK base. Modifiers are not recalculated. This is intentional — see `00-game-design-master.md` Key Design Decisions.

### 11d. New Faction Releases

A new faction is a full launch pipeline run for a new faction ID. Steps:

1. Add the new faction to the `factions` table in Supabase (faction_id, name, art_style_prefix, flavor_voice_guide, exclusive_mechanic, etc.)
2. Create `card-specs/[new_faction].csv` following the distribution tables in Section 1c
3. Run the batch pipeline for all card types
4. Review and publish

No code changes needed — the batch pipeline and review gallery work for any faction_id.

### 11e. Seasonal Event Avatars

```bash
# Generate 2 event avatars for an Ironwright seasonal event
npx ts-node scripts/generate-batch.ts --faction=ironwright --type=avatar --count=2
```

Avatar generation uses the same FLUX Dev pipeline with a modified prompt (portrait of a humanoid character rather than a creature). The review gallery works identically.

---

## 12. QA and Testing

### 12a. Automated Balance Simulation

After publishing a batch, the pipeline automatically runs a quick balance simulation:

```bash
npx ts-node scripts/balance-sim.ts --faction=ironwright --games=1000
```

This runs 1,000 simulated games using the production battle engine (the Railway game server) with randomized decks weighted toward newly published cards. It outputs:

- Win rate per card (when drawn)
- Average turns on board per creature
- Average damage dealt per creature
- Cards flagged as outliers (win rate >60% or <30%, or average damage 2× faction median)

**Outlier cards are not automatically changed.** The report is shown to the owner in the admin dashboard. The owner decides whether to create a balance patch.

**Cost:** $0 (computation only, runs on Railway server)

**Time:** About 5 minutes for 1,000 simulations of the full 358-card pool.

### 12b. Art Quality Gates (Automated)

These run during batch generation (Section 4a). No additional manual step needed.

The pipeline also runs a **faction consistency check** after each batch:

1. Loads all previously approved cards for the faction from Supabase
2. Computes average color histogram for the faction's approved art
3. Compares the new batch's art against the faction average
4. Flags cards whose color histogram deviates more than 40% from the faction median

This catches cards that look like the wrong faction (e.g., a Fey card with fire-orange tones). Flagged cards get a "FACTION STYLE MISMATCH?" warning badge in the gallery — the owner reviews and decides.

### 12c. Regression Testing

Claude Code builds and maintains a test suite at `tests/`. These run automatically:

- On every code commit via GitHub Actions
- Before every `npx ts-node scripts/publish-batch.ts` command (blocks publish if any test fails)

**Test categories:**

| Suite | What It Tests | Run Time |
|---|---|---|
| `tests/balance/` | PP budget checks, stat ranges, keyword legality for all card templates in Supabase | ~30 seconds |
| `tests/combat/` | Unit tests for all 7 keyword interactions (all combinations from `01-battle-mechanics.md` Section 4 interaction matrix) | ~10 seconds |
| `tests/instability/` | Player instability calculation formula, clamping rules, evolution instability changes | ~5 seconds |
| `tests/evolution/` | Evolution energy thresholds, shard tier assignments, PP growth calculations | ~5 seconds |
| `tests/deck/` | Deck construction validation (20 cards, single faction, max 2 copies, Legendary limits) | ~5 seconds |
| `tests/events/` | All 16 events (8 Order, 8 Chaos) resolve correctly, trigger sequence order | ~10 seconds |
| **Total** | | **~65 seconds** |

New content (new cards, balance patches) does not require new tests unless a new keyword or mechanic is introduced.

### 12d. Pre-Release Checklist (Run Before Each Quarterly Release)

This is a single command:

```bash
npx ts-node scripts/pre-release-check.ts
```

It runs:
1. All regression tests (Section 12c)
2. Balance validation for all card templates (PP check, stat ranges)
3. Distribution check: instability and CM cost percentages within ±5% of targets
4. Duplicate name check: no two cards in the same faction share a name
5. Art URL reachability check: HTTP HEAD request to every `art_url` in Supabase (verifies R2 files exist)
6. Outputs a pass/fail report. All checks must pass before publishing a quarterly release.

---

## 13. Cost Projections

### 13a. Launch Content (One-Time)

| Item | Cost |
|---|---|
| 358 card arts + 5 icon candidates (fal.ai, including ~20% regeneration) | ~$10.89 |
| 358 card names + flavor texts (OpenAI Batch API) | ~$0.02 |
| Cloudflare R2 storage and CDN (first year) | ~$0.00 (under free tier) |
| App Store screenshots via Fastlane | ~$0.00 |
| Privacy policy + ToS on Cloudflare Pages | ~$0.00 |
| **Total launch content generation** | **~$11.54** |

### 13b. First Month Infrastructure

| Item | Cost |
|---|---|
| Apple Developer Program | $99.00 |
| Railway (batch pipeline server) | $5.00 |
| Supabase (free tier during development) | $0.00 |
| Cloudflare R2 (free tier) | $0.00 |
| PostHog (free tier) | $0.00 |
| **Total first month** | **$104.00** |

### 13c. Total to Launch

| Item | Cost |
|---|---|
| Launch content generation | $11.54 |
| First month infrastructure | $104.00 |
| **Total** | **$115.54** |
| **Budget remaining ($300 limit)** | **$184.46** |

### 13d. Quarterly Content Releases

| Item | Cost per Quarter |
|---|---|
| 60-90 new card arts (fal.ai) | ~$2.00-$2.75 |
| Text generation (OpenAI) | <$0.01 |
| **Total per quarterly release** | **~$2-3** |

### 13e. Annual Content Costs

| Item | Cost per Year |
|---|---|
| 4 quarterly card releases | ~$8-12 |
| 1 new faction (120 cards, same as launch pipeline) | ~$3.75 |
| 12 seasonal event avatars (~1-2 per event × 8 events) | ~$0.30 |
| Balance patches | $0.00 (no AI calls, only Supabase edits) |
| **Total annual content AI cost** | **~$12-16** |

### 13f. Player-Driven Evolution Costs (Per-Player, Monthly)

| Tier | Evolutions/Month (avg) | Cost per Evolution | Monthly AI Cost per Player |
|---|---|---|---|
| Free | 3-5 | ~$0.02 (FLUX Kontext Dev) | ~$0.06-0.10 |
| Mid ($6.99/mo) | 8-12 | ~$0.04 (FLUX Kontext Pro) | ~$0.32-0.48 |
| High ($12.99/mo) | 15-25 | ~$0.04-0.08 (FLUX Kontext Pro) | ~$0.60-2.00 |

Subscription revenue covers AI evolution costs at 8-20× margin. See `09-monetization-details.md` for full unit economics.

---

## 14. Risks and Mitigations

| Risk | Mitigation |
|---|---|
| fal.ai API rate limits slow batch generation | Pipeline uses 10 concurrent requests max (stays under rate limits). If rate limited, pipeline auto-waits and retries with exponential backoff (2s, 4s, 8s, 16s, 32s max). |
| fal.ai downtime during a batch run | Batch state is saved to manifest JSON after every card. Resume with `--resume --batch=BATCH_ID` when service is restored. No cards are lost. |
| OpenAI Batch API takes longer than 24 hours | Rare but possible. The batch pipeline checks status every hour. If a batch is still pending after 48 hours, it automatically falls back to real-time API calls for the remaining cards. |
| High AI art rejection rate (>30%) | Strengthen prompts in `card-specs/[faction].csv` creature archetype descriptions. Add more specificity to the archetype field. Cost impact: $0.025 per extra regeneration — not a financial risk. |
| Balance errors in spec CSV | The pre-generation validation pass catches all PP errors before any API calls are made. Fix the CSV and re-run. |
| Duplicate names across factions | The pre-release check catches duplicates. Within a faction, names must be unique. Cross-faction duplicates are acceptable. |
| Cloudflare R2 upload failure | Pipeline retries 3 times with 5-second delays. If all 3 fail, the card is marked "UPLOAD_FAILED" in the manifest and gallery. Owner retries via "Retry Upload" button. |
| App Store Review rejection | App Store Connect Metadata must be complete before submission. Age rating 9+ is appropriate for the game's content. No gambling mechanics (no randomized card packs). |
| Budget overrun | Current estimate $115.54 against $300 limit leaves $184.46 headroom. The largest single cost is the Apple Developer Program ($99/year) which is unavoidable. Monitor fal.ai spend via dashboard during generation runs. |

---

## 15. Future Content Ideas

### 15a. Community Card Submissions (Year 2+)

Players submit creature archetype ideas (text description only) via an in-app form. Approved submissions go into the spec CSV for the next quarterly release. The submitting player's in-game name appears in the card's flavor text as an easter egg. This is a Year 2 feature once the pipeline is stable.

### 15b. Faction Crossover Events (Year 2+)

Limited-time events where cards from multiple factions can coexist in a single deck. No new card generation needed — existing cards are temporarily allowed cross-faction. A feature flag in the game server enables this during the event window.

### 15c. AI-Generated Quest Narratives (Year 3+)

Use GPT-4o Mini to generate personalized quest flavor text based on the player's faction and avatar. Text generation cost is negligible; the design challenge is quality consistency. Year 3 R&D.

---

## Appendix A: Faction Art Prompt Prefixes (Full Strings)

These are stored in the `factions` table in Supabase (`art_style_prefix` column) and pulled dynamically by the batch generation script. They are reproduced here for reference. The canonical definitions are in `03-prompt-templates.md` Section 1.2.

**The Ironwright Collective:**
```
steampunk mechanical creature, brass and copper materials, exposed gears and clockwork mechanisms, riveted metal plating, steam vents, intricate precision engineering, industrial Victorian aesthetic, warm metallic tones with amber and rust highlights, glowing amber lenses
```

**The Fey Courts:**
```
ethereal fey fantasy creature, ancient forest setting, bioluminescent flora and glowing fungi, living wood and vine armor, mystical natural magic, soft moonlight and starlight illumination, organic flowing forms, moss and crystal accents, cool nature palette with silver and violet highlights
```

**The Demonic Kingdoms:**
```
demonic corrupted dark fantasy creature, hellfire and deep shadow, obsidian and bone construction, infernal glyphs and runes, corrupted flesh with visible strain, volcanic ash and floating embers, blood-red and deep purple-black tones, visceral menacing presence
```

---

## Appendix B: Balance Validation Checklist

Use this when manually reviewing flagged cards or when editing stats in the gallery UI. The automated pipeline runs all of these, but this is the human-readable version.

**Creature cards:**

- [ ] PP budget: ATK + HP + keyword PP cost == (CM cost × 2 + 1), tolerance ±1
- [ ] ATK and HP within stat range for CM cost (see Section 1c table)
- [ ] Instability 0-1: HP must be >= ATK
- [ ] Instability 2: HP within 1 point of ATK
- [ ] Instability 4-5: ATK must be >= HP
- [ ] Max 1 keyword at Common tier
- [ ] If keyword is Deathtouch: ATK must be 1 or 2
- [ ] ATK >= 1 and HP >= 1

**Spell cards:**

- [ ] Effect power matches CM cost (see spell ranges in Section 1c)
- [ ] Target type is valid for effect type
- [ ] Name is clear and describes the effect

**Stabilizer cards:**

- [ ] Instability modification is reasonable (±1-3 instability per turn, or fixed value between 5-15)
- [ ] HP between 2-5 (must be destroyable)
- [ ] Cannot have ATK (stabilizers don't attack)

---

## Appendix C: Prompt Assembly Reference

The batch generation script assembles prompts programmatically. This is the reference for Claude Code when building the script.

**Art prompt assembly function signature:**
```typescript
function buildArtPrompt(
  factionPrefix: string,        // from card-specs CSV faction_id -> factions table
  creatureArchetype: string,    // from card-specs CSV creature_archetype column
  instability: number,          // 0-5
  keywords: string[],           // array of keyword names
): string
```

**Text prompt assembly function signature:**
```typescript
function buildTextPrompt(
  factionId: string,            // 'ironwright' | 'fey' | 'demonic'
  creatureArchetype: string,
  cmCost: number,
  baseAttack: number,
  baseHealth: number,
  instability: number,
  keywords: string[],
): object // OpenAI chat completions request body
```

Both functions are implemented in `scripts/prompt-builder.ts`. The batch generation script imports and calls them for each card spec row.

---

## Revision Log

This section records every change made to the document.

---

**Revision date:** 2026-02-16

**What was changed and why (initial revision):**

1. **Removed generic infrastructure references.** Replaced throughout with the exact stack: fal.ai for image generation, Cloudflare R2 for storage, OpenAI for text, Supabase for database. No alternatives offered.

2. **Replaced all "automated scripts" language with exact API calls.** The original section 2b had a high-level description of the pipeline with no implementation details. Replaced with exact fal.ai and OpenAI request and response JSON (Section 3a and 3b). A developer or Claude Code can implement directly from these specs without any additional research.

3. **Added the Supabase insert call with exact field names.** Section 3d now includes the exact TypeScript insert call and the full SQL CREATE TABLE statement.

4. **Added the Cloudflare R2 upload call.** Section 3c now includes the exact TypeScript upload using the AWS SDK v3, including the bucket key structure and Cache-Control header.

5. **Replaced "internal web UI" with a specific review gallery spec.** Section 5 now specifies the exact page structure, every button and what it does technically, how the status badge system works, and how inline editing works.

6. **Added exact ts-node commands with options.** Section 6 now has the exact commands with all supported options documented.

7. **Added the card spec CSV format with column definitions and examples.** Section 7 now has the exact column names, types, constraints, and three example rows including PP validation walkthroughs.

8. **Added the exact Step-by-Step Generation Sequence.** Section 8b now has the exact bash commands for each day of the 4-day launch generation run.

9. **Updated all cost estimates to reflect fal.ai pricing.** Section 10a attributes costs specifically to fal.ai and notes to check the dashboard for current rates.

10. **Added the balance migration script.** Section 11c includes the exact SQL UPDATE statement that recalculates card instance stats after a template change.

11. **Added automated pre-release check command.** Section 12d replaces a manual checklist with a single `npx ts-node scripts/pre-release-check.ts` command.

12. **Added admin balance UI spec.** Section 11c specifies a balance admin screen in the admin dashboard web application.

13. **Removed "engineering should build this" language throughout.** Every section previously ended with "To build" status or described the tool as a future engineering task. Replaced with specific implementation instructions that Claude Code can act on directly.

14. **Corrected total card count.** The original's header said "270-375 cards" (from the initial brief) but the detailed Section 1 calculated 358. Used the consistent target of 358 cards (351 faction + 7 universal) throughout, matching INFO-3 from REVIEW.md.

---

**Revision date:** 2026-02-16 (platform update)

**What was changed and why:**

1. **Removed all React Native, Expo, and npm-run-script references.** The project is now native iOS only (Swift + SwiftUI + SpriteKit). The batch pipeline and admin dashboard are Node.js/TypeScript on the server side (Railway), which is correct and unchanged. Removed all language implying the pipeline runs inside the iOS app or via Expo tooling.

2. **Replaced `npm run` commands with `npx ts-node` commands throughout.** The batch generation, review gallery, publish, regenerate, and pre-release check commands are now expressed as direct `ts-node` script invocations (Sections 6, 8b, 11b, 12a, 12d). This matches the Railway/Node.js server-side stack without implying a client-side npm project.

3. **Added Admin Dashboard clarification.** Added a clear statement at the top of the document and in Section 5a: the review gallery is a web application in the admin dashboard (Railway-deployed), not part of the iOS app.

4. **Changed image format from PNG to WebP for card art.** The batch generation pipeline now produces WebP (matching the `output_format: "webp"` used in `03-prompt-templates.md` Section 1.1 and the evolution pipeline). Changed `image_size` from `square_hd` (1024×1024) to `portrait_4_3` (768×1024) to match the portrait aspect ratio used for card art at all tiers per `03-prompt-templates.md` Section 1.1. App icon remains PNG as required by Apple.

5. **Added Section 9 — App Store Asset Generation.** New section covering all assets required for App Store submission: app icon generation via fal.ai with exact prompt and script, screenshots via Fastlane snapshot (not Expo) with full Snapfile and Swift UI test code, privacy policy and ToS on Cloudflare Pages, App Store description copy template, age rating questionnaire answers, privacy nutrition labels, and a complete App Store launch checklist.

6. **Added resumable pipeline with JSON manifest.** Section 2c now fully specifies the manifest JSON structure, all card status values, resume behavior (skipping completed cards on restart), and the exponential backoff retry function for fal.ai errors (2s → 4s → 8s → 16s → 32s cap). The pipeline can be killed and re-run at any point and picks up where it left off.

7. **Updated cost section to include App Store assets and $300 budget analysis.** Section 10a now includes app icon generation cost ($0.13), screenshots at $0.00 (Fastlane/Simulator), and static pages at $0.00 (Cloudflare Pages free tier). Section 13c shows total-to-launch cost of ~$115.54 against the $300 budget, leaving $184.46 headroom. Subscription prices updated to final values: $6.99 Mid, $12.99 High (matching WARN-5 from REVIEW.md).

8. **Replaced screenshot tooling.** Removed any language referencing Expo or EAS screenshot tools. Screenshots now use Fastlane snapshot driven by Xcode UI Tests, with exact `Snapfile` configuration and Swift `ScreenshotTests` class. Section 9b contains complete, copy-paste-ready code.

9. **Added App Store risk to risks table.** Section 14 now includes a row for App Store Review rejection risk with mitigations.

10. **Fixed flavor text length constraint.** Changed from 140 characters (in the first revision) to 120 characters throughout, matching the canonical constraint in `03-prompt-templates.md` Section 2.2.

11. **Aligned image format references.** Section 4a resolution check updated from 1024×1024 to 768×1024 to match the `portrait_4_3` image size. File size range updated from 100KB–4MB to 50KB–3MB to match WebP's better compression.

12. **Removed REVIEW.md INFO-3 inconsistency.** Card counts throughout now consistently use the final figures (100 creatures + 17 spells per faction = 117 per faction, 351 faction cards + 7 universal stabilizers = 358 total). Faction stabilizers removed from per-faction counts — all stabilizers are universal per protected docs 00 and 01. The "270-375" range from the original brief is gone.
