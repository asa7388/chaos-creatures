// Chaos Creatures Admin Dashboard — Login Page
// TODO: Implement password login form in Wave 2
// Single shared password auth (owner only).

export default function LoginPage() {
  return (
    <div>
      <h1>Admin Login</h1>
      {/* TODO: Password input + submit → POST /api/auth */}
    </div>
  );
}
