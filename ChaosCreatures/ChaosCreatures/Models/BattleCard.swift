// BattleCard.swift
// Chaos Creatures
// TODO: Implement in Wave 1
// Runtime battle representation of a card (ephemeral, not persisted).

import Foundation

struct BattleCard: Identifiable {
    let id: UUID
    // TODO: Add battle-specific fields (current HP, buffs, debuffs, board position)
}
